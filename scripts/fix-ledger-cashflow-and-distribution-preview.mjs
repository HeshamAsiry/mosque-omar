import fs from 'node:fs';
const path='src/main.jsx';let s=fs.readFileSync(path,'utf8');
const replace=(name,next,fn)=>{const a=s.indexOf(`function ${name}(`),b=s.indexOf(next,a);if(a<0||b<0)throw new Error(name+' boundary not found');s=s.slice(0,a)+fn+s.slice(b)};
const monthly=`function MonthlyPayments({cases,amounts}){const today=monthStart(new Date()),currentMonth=monthKey(today),[month,setMonth]=useState(currentMonth),[rows,setRows]=useState([]),[busy,setBusy]=useState(false),[filter,setFilter]=useState('all'),[search,setSearch]=useState(''),[donationBalance,setDonationBalance]=useState(0);const active=cases.filter(c=>c.status==='active'&&(filter==='all'||c.case_type===filter)&&String(c.mother_name||'').toLocaleLowerCase('ar-EG').includes(search.trim().toLocaleLowerCase('ar-EG'))).sort((a,b)=>String(a.mother_name||'').localeCompare(String(b.mother_name||''),'ar',{sensitivity:'base'}));async function load(){const[{data:p},{data:t}]=await Promise.all([supabase.from('monthly_payments').select('*').gte('month',PAYMENT_START_MONTH),supabase.from('fund_transactions').select('amount,transaction_type').eq('fund_type','donation')]);setRows(p||[]);setDonationBalance((t||[]).reduce((n,x)=>n+(x.transaction_type==='income'?Number(x.amount):-Number(x.amount)),0))}useEffect(()=>{load()},[month,cases.length]);useEffect(()=>{const sub=supabase.channel('monthly-ledger-cashflow').on('postgres_changes',{event:'*',schema:'public',table:'monthly_payments'},load).on('postgres_changes',{event:'*',schema:'public',table:'fund_transactions'},load).subscribe();return()=>supabase.removeChannel(sub)},[]);const rowFor=id=>rows.find(x=>x.case_id===id&&x.month===month),isPaid=id=>!!rowFor(id)?.paid,baseFor=c=>Number(rowFor(c.id)?.base_amount??rowFor(c.id)?.amount??caseAmount(c,amounts)),extraFor=c=>Number(rowFor(c.id)?.extra_amount||0),totalFor=c=>Number(rowFor(c.id)?.total_amount??baseFor(c)+extraFor(c)),paidBase=active.filter(c=>isPaid(c.id)),baseTotal=active.reduce((n,c)=>n+baseFor(c),0),extraTotal=active.reduce((n,c)=>n+extraFor(c),0),paidTotal=paidBase.reduce((n,c)=>n+totalFor(c),0);async function toggle(c){const paid=!isPaid(c.id);setBusy(true);const{data,error}=await supabase.rpc('set_monthly_payment_paid',{p_case_id:c.id,p_month:month,p_paid:paid});if(error)alert(error.message);else await load();setBusy(false)}return <section className="panel payment-page"><div className="panel-head"><div><h2>سجل الصرف الشهري</h2><p>كل قبض أساسي يُسجل كصرف من صندوق التبرعات، والمتبقي يظل رصيدًا مرحلًا للشهر التالي.</p></div><span>{active.length} حالة</span></div><div className="payment-tools"><div className="payment-search"><span>⌕</span><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="بحث سريع باسم الأسرة..."/></div><div className="payment-monthbar"><button disabled={month<=shiftMonth(currentMonth,-24)} onClick={()=>setMonth(shiftMonth(month,-1))}>‹</button><strong>{monthLabel(month)}</strong><button disabled={month>=currentMonth} onClick={()=>setMonth(shiftMonth(month,1))}>›</button><button className="secondary" disabled={month===currentMonth} onClick={()=>setMonth(currentMonth)}>الشهر الحالي</button></div></div><div className="fund-balance-card"><span>الرصيد المتاح لصرف المساعدات</span><strong>{money(Math.max(0,donationBalance))}</strong><small>هذا الرصيد = الإيرادات − المصروفات الفعلية، وأي متبقٍ يرحّل تلقائيًا للشهر التالي.</small></div><div className="payment-ledger-summary"><article><span>إجمالي الأساسي</span><b>{money(baseTotal)}</b></article><article><span>إجمالي الإضافي</span><b>{money(extraTotal)}</b></article><article className="highlight"><span>إجمالي المستحق</span><b>{money(baseTotal+extraTotal)}</b></article><article><span>إجمالي المقبوض</span><b>{money(paidTotal)}</b></article></div><div className="stats payment-stats"><article className="stat"><span>تم القبض</span><strong>{paidBase.length} / {active.length}</strong></article><article className="stat"><span>لم يتم القبض</span><strong>{Math.max(0,active.length-paidBase.length)}</strong></article><article className="stat"><span>إضافي للأسر</span><strong>{active.filter(c=>extraFor(c)>0).length}</strong></article></div><div className="case-filters"><button className={filter==='all'?'filter active':'filter'} onClick={()=>setFilter('all')}>الكل</button><button className={filter==='aid'?'filter active':'filter'} onClick={()=>setFilter('aid')}>مساعدات</button><button className={filter==='orphan_sponsorship'?'filter active':'filter'} onClick={()=>setFilter('orphan_sponsorship')}>كفالة أيتام</button></div><div className="payment-table-wrap"><table className="payment-table"><thead><tr><th>الرقم</th><th>الاسم</th><th>النوع</th><th>الأساسي</th><th>الإضافي</th><th>الإجمالي</th><th>حالة القبض</th></tr></thead><tbody>{active.map((c,i)=>{const paid=isPaid(c.id),extra=extraFor(c),base=baseFor(c),total=totalFor(c);return <tr id={'payment-row-'+c.id} key={c.id}><td>{i+1}</td><td><b>{c.mother_name}</b></td><td>{c.case_type==='aid'?'مساعدات':'كفالة أيتام'}</td><td>{money(base)}</td><td className={extra>0?'extra-amount':''}>{extra>0?money(extra):'—'}</td><td><b>{money(total)}</b></td><td><button disabled={busy} className={paid?'payment-toggle paid':'payment-toggle'} onClick={()=>toggle(c)}>{paid?'✓ تم القبض':'○ لم يقبض'}</button></td></tr>})}</tbody></table></div>{active.length===0&&<div className="empty">لا توجد نتائج مطابقة.</div>}</section>}`;
replace('MonthlyPayments','\nfunction PaymentHistory',monthly);
const external=`function ExternalDistribution({cases}){const[balance,setBalance]=useState(0),[external,setExternal]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState('');const active=cases.filter(c=>c.status==='active'),aidCases=active.filter(c=>c.case_type==='aid'),kidRecipients=active.filter(c=>c.case_type==='orphan_sponsorship').flatMap(c=>eligibleKids(c).map(k=>({caseId:c.id,childId:k.id,childName:k.name||'طفل',motherName:c.mother_name,kind:'orphan'}))),recipients=[...aidCases.map(c=>({caseId:c.id,childId:null,childName:null,motherName:c.mother_name,kind:'aid'})),...kidRecipients];async function load(){const{data}=await supabase.from('fund_transactions').select('amount,transaction_type').eq('fund_type','external');setBalance((data||[]).reduce((n,x)=>n+(x.transaction_type==='income'?Number(x.amount):-Number(x.amount)),0))}useEffect(()=>{load();const sub=supabase.channel('external-preview').on('postgres_changes',{event:'*',schema:'public',table:'fund_transactions'},load).subscribe();return()=>supabase.removeChannel(sub)},[]);const source=Math.max(0,Math.floor(Number(external)||0)||balance),units=Math.floor(source/50),totalWeight=aidCases.length*4+kidRecipients.length*3;let allocations=recipients.map(r=>({...r,ideal:totalWeight?units*(r.kind==='aid'?4:3)/totalWeight:0,units:0}));if(units>=allocations.length){allocations=allocations.map(r=>({...r,units:Math.floor(r.ideal)}));let left=units-allocations.reduce((n,r)=>n+r.units,0);while(left>0){const next=allocations.reduce((best,r)=>r.ideal-r.units>best.ideal-best.units?r:best,allocations[0]);if(!next)break;next.units++;left--}}else{[...allocations].sort((a,b)=>b.ideal-a.ideal).slice(0,units).forEach(r=>r.units=1)}allocations=allocations.map(r=>({...r,amount:r.units*50}));const distributed=allocations.reduce((n,r)=>n+r.amount,0),remainder=Math.max(0,source-distributed),aidTotal=allocations.filter(r=>r.kind==='aid').reduce((n,r)=>n+r.amount,0),orphanTotal=allocations.filter(r=>r.kind==='orphan').reduce((n,r)=>n+r.amount,0);async function approve(){if(!distributed||busy)return;setBusy(true);setMessage('');const{data:{user}}=await supabase.auth.getUser();const{data:d,error}=await supabase.from('external_distributions').insert({distribution_date:new Date().toISOString().slice(0,10),source_amount:source,distributed_amount:distributed,remainder,notes:'توزيع خارجي — جاهز للاعتماد',created_by:user?.id,status:'draft'}).select().single();if(error){alert(error.message);setBusy(false);return}const items=allocations.filter(r=>r.amount>0).map(r=>({distribution_id:d.id,case_id:r.caseId,child_id:r.childId,recipient_type:r.kind==='aid'?'aid':'orphan',amount:r.amount}));const{error:itemError}=await supabase.from('external_distribution_items').insert(items);if(itemError){alert(itemError.message);setBusy(false);return}const{data:result,error:approvalError}=await supabase.rpc('approve_external_distribution',{p_distribution_id:d.id});if(approvalError){alert(approvalError.message);setBusy(false);return}await audit('external_distribution_approved','external_distribution',d.id,result);setMessage('تم اعتماد التوزيع وتسجيل الصرف والإضافي في سجل الشهر تلقائيًا.');setExternal('');await load();setBusy(false)}return <section className="panel external-distribution-page"><div className="panel-head"><div><h2>التوزيع الخارجي</h2><p>قبل الاعتماد يمكنك مراجعة نصيب كل أسرة وكل طفل بالتفصيل. النسبة المستهدفة: المساعدة 100 مقابل الطفل 75.</p></div><span className="status-pill">{balance>0?'رصيد متاح':'لا يوجد رصيد'}</span></div><div className="external-balance-hero"><div><span>الرصيد الخارجي الحالي</span><strong>{money(balance)}</strong></div><div><span>المبلغ المراد توزيعه</span><input type="number" min="0" step="50" value={external} onChange={e=>setExternal(e.target.value)} placeholder="اتركه فارغًا لاستخدام كامل الرصيد"/></div></div><div className="stats"><article className="stat"><span>حالات المساعدات</span><strong>{aidCases.length}</strong></article><article className="stat"><span>الأطفال المؤهلون</span><strong>{kidRecipients.length}</strong></article><article className="stat"><span>المبلغ الموزع</span><strong>{money(distributed)}</strong></article><article className="stat"><span>المتبقي للترحيل</span><strong>{money(remainder)}</strong></article></div><div className="distribution-summary-grid"><div><span>إجمالي المساعدات</span><b>{money(aidTotal)}</b></div><div><span>إجمالي الأيتام</span><b>{money(orphanTotal)}</b></div><div><span>النسبة</span><b>100 : 75</b></div><div><span>وحدة التوزيع</span><b>50 جنيه</b></div></div><div className="distribution-recipients"><div className="distribution-recipients-head"><b>تفاصيل نصيب المستحقين</b><span>{allocations.filter(r=>r.amount>0).length} مستحق</span></div><div className="distribution-recipient-table"><div><b>المستحق</b><b>الفئة</b><b>النصيب</b></div>{allocations.filter(r=>r.amount>0).map((r,i)=><div key={r.childId||r.caseId+'-'+i}><span>{r.kind==='aid'?r.motherName:r.childName}</span><span>{r.kind==='aid'?'مساعدة':'طفل يتيم'}</span><strong>{money(r.amount)}</strong></div>)}</div></div>{distributed>0?<><div className="distribution-review"><div><b>قبل الاعتماد</b><span>سيتم خصم {money(distributed)} فقط من الصندوق الخارجي.</span></div><div><b>أثر الاعتماد</b><span>سيضاف نفس النصيب لكل مستحق في شهر {monthLabel(monthKey(new Date()))}.</span></div></div><button className="primary external-approve" disabled={busy} onClick={approve}>{busy?'جارٍ اعتماد التوزيع...':'اعتماد التوزيع وخصم المبلغ'}</button></>:<div className="note">لا يوجد مبلغ قابل للتوزيع حاليًا.</div>}{message&&<div className="note success">{message}</div>}</section>}`;
replace('ExternalDistribution','\ncreateRoot(',external);
fs.writeFileSync(path,s);console.log('Monthly cash flow now uses the donation ledger and external distribution shows each recipient share.');