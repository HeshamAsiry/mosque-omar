import fs from 'node:fs';

const path='src/main.jsx';
let s=fs.readFileSync(path,'utf8');
if(!s.includes("import'./funds.css';")) s=s.replace("import'./branding.css';","import'./branding.css';import'./funds.css';");
if(!s.includes("['funds','الصندوق','Wallet']")) s=s.replace("['external','التوزيع الخارجي','◇']", "['funds','الصندوق','Wallet'],['external','التوزيع الخارجي','◇']");
if(!s.includes("page==='funds'&&<FundsManagement")) s=s.replace("{page==='external'&&<ExternalDistribution cases={cases}/>} ","{page==='funds'&&<FundsManagement cases={cases} amounts={amounts}/>} {page==='external'&&<ExternalDistribution cases={cases}/>} ");

const mpStart=s.indexOf('function MonthlyPayments({cases,amounts}){');
const phStart=s.indexOf('function PaymentHistory({caseId}){',mpStart);
if(mpStart>=0&&phStart>mpStart){
const fn=`function MonthlyPayments({cases,amounts}){const today=monthStart(new Date()),[month,setMonth]=useState(monthKey(today)),[rows,setRows]=useState([]),[busy,setBusy]=useState(false),[filter,setFilter]=useState('all'),[donationIncome,setDonationIncome]=useState(0);const active=cases.filter(c=>c.status==='active'&&(filter==='all'||c.case_type===filter)).sort((a,b)=>String(a.mother_name||'').localeCompare(String(b.mother_name||''),'ar',{sensitivity:'base'}));const currentMonth=monthKey(today);async function load(){const[{data:p},{data:t}]=await Promise.all([supabase.from('monthly_payments').select('*').gte('month',PAYMENT_START_MONTH),supabase.from('fund_transactions').select('amount,transaction_type').eq('fund_type','donation')]);setRows(p||[]);setDonationIncome((t||[]).reduce((n,x)=>n+(x.transaction_type==='income'?Number(x.amount):0),0))}useEffect(()=>{load()},[month,cases.length]);useEffect(()=>{const sub=supabase.channel('monthly-payments-funds').on('postgres_changes',{event:'*',schema:'public',table:'monthly_payments'},load).on('postgres_changes',{event:'*',schema:'public',table:'fund_transactions'},load).subscribe();return()=>supabase.removeChannel(sub)},[]);const amountFor=id=>caseAmount(cases.find(c=>c.id===id),amounts);const paidRows=rows.filter(x=>x.paid);const paidTotalAll=paidRows.reduce((n,x)=>n+Number(x.amount??amountFor(x.case_id)),0);const donationBalance=donationIncome-paidTotalAll;const isPaid=id=>rows.some(x=>x.case_id===id&&x.month===month&&x.paid);async function toggle(c){setBusy(true);const paid=!isPaid(c.id),amount=caseAmount(c,amounts);if(paid&&donationBalance<amount){alert('الرصيد المتاح في صندوق التبرعات لا يكفي لصرف هذه الحالة.');setBusy(false);return}const{data:{user}}=await supabase.auth.getUser();const{error}=await supabase.from('monthly_payments').upsert({case_id:c.id,month,paid,amount:paid?amount:null,paid_at:paid?new Date().toISOString():null,paid_by:paid?user?.id:null,updated_at:new Date().toISOString()},{onConflict:'case_id,month'});if(error)alert(error.message);else{await audit(paid?'payment_marked':'payment_unmarked','monthly_payment',c.id,{month,paid,amount:paid?amount:0});await load()}setBusy(false)}const paidCount=active.filter(c=>isPaid(c.id)).length,total=active.reduce((n,c)=>n+caseAmount(c,amounts),0),paidTotal=active.filter(c=>isPaid(c.id)).reduce((n,c)=>n+caseAmount(c,amounts),0),unpaidTotal=total-paidTotal;return <section className="panel payment-page"><div className="panel-head"><div><h2>سجل الصرف الشهري</h2><p>حدد الشهر ثم علّم الحالات التي تم صرف المساعدة لها.</p></div><span>{active.length} حالة</span></div><div className="fund-balance-card"><span>الرصيد المتاح لصرف المساعدات</span><strong>{money(Math.max(0,donationBalance))}</strong><small>إجمالي تبرعات الجمعة: {money(donationIncome)} · المصروف: {money(paidTotalAll)}</small></div><div className="payment-monthbar"><button disabled={month<=shiftMonth(currentMonth,-24)} onClick={()=>setMonth(shiftMonth(month,-1))}>‹</button><strong>{monthLabel(month)}</strong><button disabled={month>=currentMonth} onClick={()=>setMonth(shiftMonth(month,1))}>›</button><button className="secondary" disabled={month===currentMonth} onClick={()=>setMonth(currentMonth)}>الشهر الحالي</button></div><div className="stats payment-stats"><article className="stat"><span>إجمالي المستحق</span><strong>{money(total)}</strong></article><article className="stat"><span>تم الصرف</span><strong>{money(paidTotal)}</strong></article><article className="stat"><span>لم يتم الصرف</span><strong>{money(unpaidTotal)}</strong></article><article className="stat"><span>تم القبض</span><strong>{paidCount} / {active.length}</strong></article></div><div className="case-filters"><button className={filter==='all'?'filter active':'filter'} onClick={()=>setFilter('all')}>الكل</button><button className={filter==='aid'?'filter active':'filter'} onClick={()=>setFilter('aid')}>مساعدات</button><button className={filter==='orphan_sponsorship'?'filter active':'filter'} onClick={()=>setFilter('orphan_sponsorship')}>كفالة أيتام</button></div><div className="payment-table-wrap"><table className="payment-table"><thead><tr><th>الرقم</th><th>الاسم</th><th>النوع</th><th>المبلغ</th><th>حالة القبض</th></tr></thead><tbody>{active.map((c,i)=>{const paid=isPaid(c.id);return <tr id={'payment-row-'+c.id} key={c.id}><td>{i+1}</td><td><b>{c.mother_name}</b></td><td>{c.case_type==='aid'?'مساعدات':'كفالة أيتام'}</td><td>{money(caseAmount(c,amounts))}</td><td><button disabled={busy} className={paid?'payment-toggle paid':'payment-toggle'} onClick={()=>toggle(c)}>{paid?'✓ تم القبض':'○ لم يقبض'}</button></td></tr>})}</tbody></table></div>{active.length===0&&<div className="empty">لا توجد حالات نشطة.</div>}</section>}
`;
s=s.slice(0,mpStart)+fn+s.slice(phStart);
}

const extStart=s.indexOf('function ExternalDistribution(');
const createRootIdx=s.indexOf('\ncreateRoot(',extStart);
if(extStart>=0&&createRootIdx>extStart){
const fn=`function ExternalDistribution({cases}){const[balance,setBalance]=useState(0),[busy,setBusy]=useState(false),[message,setMessage]=useState('');const active=cases.filter(c=>c.status==='active'),aidCases=active.filter(c=>c.case_type==='aid'),kids=active.filter(c=>c.case_type==='orphan_sponsorship').flatMap(c=>eligibleKids(c).map(k=>({caseId:c.id,childId:k.id,kind:'orphan'}))),recipients=[...aidCases.map(c=>({caseId:c.id,kind:'aid'})),...kids],load=async()=>{const[{data:inc},{data:dist}]=await Promise.all([supabase.from('fund_transactions').select('amount').eq('fund_type','external').eq('transaction_type','income'),supabase.from('external_distributions').select('distributed_amount')]);const income=(inc||[]).reduce((n,x)=>n+Number(x.amount||0),0),spent=(dist||[]).reduce((n,x)=>n+Number(x.distributed_amount||0),0);setBalance(Math.max(0,income-spent))};useEffect(()=>{load();const sub=supabase.channel('external-fund').on('postgres_changes',{event:'*',schema:'public',table:'fund_transactions'},load).on('postgres_changes',{event:'*',schema:'public',table:'external_distributions'},load).subscribe();return()=>supabase.removeChannel(sub)},[]);const units=balance>=50?Math.floor(balance/50):0,totalWeight=aidCases.length*4+kids.length*3;const allocations=recipients.map(r=>({...r,ideal:totalWeight?units*50*(r.kind==='aid'?4:3)/totalWeight:0,amount:0}));const rounded=allocations.map(r=>({...r,amount:Math.floor(r.ideal/50)*50}));const distributed=rounded.reduce((n,r)=>n+r.amount,0),remainder=balance-distributed;async function approve(){if(!distributed||busy)return;setBusy(true);setMessage('');const{data:{user}}=await supabase.auth.getUser();const{data:d,error}=await supabase.from('external_distributions').insert({source_amount:balance,distributed_amount:distributed,remainder,created_by:user?.id}).select().single();if(error){alert(error.message);setBusy(false);return}const items=rounded.filter(r=>r.amount>0).map(r=>({distribution_id:d.id,case_id:r.caseId,child_id:r.childId||null,recipient_type:r.kind,amount:r.amount}));if(items.length){const{error:itemError}=await supabase.from('external_distribution_items').insert(items);if(itemError){alert(itemError.message);setBusy(false);return}}await audit('external_distribution','external_distribution',d.id,{source_amount:balance,distributed_amount:distributed,remainder});setMessage('تم اعتماد التوزيع، والمتبقي محفوظ للترحيل للشهر القادم.');await load();setBusy(false)}return <section className="panel"><div className="panel-head"><div><h2>التوزيع الخارجي</h2><p>يُستخدم رصيد المبالغ الخارجية فقط. نصيب الأيتام = ٣/٤ من نصيب المساعدة، والتقريب دائمًا إلى المبلغ الأصغر.</p></div></div><div className="fund-balance-card external"><span>الرصيد الخارجي المتاح</span><strong>{money(balance)}</strong><small>أي مبلغ متبقٍ لا يُفقد، بل يظل في الرصيد ويُرحّل للشهر القادم.</small></div><section className="stats"><article className="stat"><span>حالات المساعدات</span><strong>{aidCases.length}</strong></article><article className="stat"><span>الأطفال المؤهلون</span><strong>{kids.length}</strong></article><article className="stat"><span>المبلغ الموزع</span><strong>{money(distributed)}</strong></article><article className="stat"><span>المتبقي للترحيل</span><strong>{money(remainder)}</strong></article></section><div className="distribution-table"><div><b>الفئة</b><b>التوزيع المقترح</b><b>الإجمالي</b></div><div><span>المساعدات</span><span>{money(rounded.filter(r=>r.kind==='aid').reduce((n,r)=>n+r.amount,0))} إجماليًا</span><span>{aidCases.length} مستحق</span></div><div><span>الأيتام</span><span>{money(rounded.filter(r=>r.kind==='orphan').reduce((n,r)=>n+r.amount,0))} إجماليًا</span><span>{kids.length} طفل</span></div></div>{distributed>0&&<button className="primary"disabled={busy} onClick={approve}>{busy?'جارٍ اعتماد التوزيع...':'اعتماد التوزيع وخصم المبلغ'}</button>}{message&&<div className="note success">{message}</div>}{balance<50&&<div className="note">الرصيد الحالي أقل من 50 جنيه، لذلك لن يتم توزيع أي مبلغ حتى يزداد الرصيد.</div>}</section>}`;
s=s.slice(0,extStart)+fn+s.slice(createRootIdx);
}

if(!s.includes('function FundsManagement({cases,amounts}){')){
const marker='function Reports({cases,amounts}){';const i=s.indexOf(marker);
if(i>=0){const fn=`function FundsManagement({cases,amounts}){const[transactions,setTransactions]=useState([]),[date,setDate]=useState(new Date().toISOString().slice(0,10)),[type,setType]=useState('donation'),[amount,setAmount]=useState(''),[description,setDescription]=useState(''),[busy,setBusy]=useState(false);async function load(){const{data,error}=await supabase.from('fund_transactions').select('*').order('transaction_date',{ascending:false}).order('created_at',{ascending:false}).limit(200);if(error){console.error(error);return}setTransactions(data||[])}useEffect(()=>{load();const sub=supabase.channel('fund-transactions-page').on('postgres_changes',{event:'*',schema:'public',table:'fund_transactions'},load).subscribe();return()=>supabase.removeChannel(sub)},[]);const donations=transactions.filter(x=>x.fund_type==='donation'&&x.transaction_type==='income').reduce((n,x)=>n+Number(x.amount||0),0),external=transactions.filter(x=>x.fund_type==='external'&&x.transaction_type==='income').reduce((n,x)=>n+Number(x.amount||0),0);async function add(){const n=Math.floor(Number(amount)||0);if(n<=0){alert('أدخل مبلغًا صحيحًا.');return}setBusy(true);const{data:{user}}=await supabase.auth.getUser();const{error}=await supabase.from('fund_transactions').insert({transaction_date:date,fund_type:type,transaction_type:'income',amount:n,description:description.trim()||null,created_by:user?.id});if(error)alert(error.message);else{await audit('fund_income_added','fund_transaction',null,{fund_type:type,amount:n,date});setAmount('');setDescription('');await load()}setBusy(false)}return <section className="panel funds-page"><div className="panel-head"><div><h2>الصندوق المالي</h2><p>سجل تبرعات الجمعة والمبالغ الخارجية، وتابع الرصيد المتاح قبل الصرف.</p></div></div><div className="stats"><article className="stat"><span>إجمالي تبرعات الجمعة</span><strong>{money(donations)}</strong></article><article className="stat"><span>إجمالي المبالغ الخارجية</span><strong>{money(external)}</strong></article></div><div className="fund-form"><label>التاريخ<input type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>نوع الإيراد<select value={type} onChange={e=>setType(e.target.value)}><option value="donation">تبرعات الجمعة</option><option value="external">مبلغ خارجي</option></select></label><label>المبلغ<input type="number" min="1" step="1" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="أدخل المبلغ"/></label><label className="wide">بيان مختصر<input value={description} onChange={e=>setDescription(e.target.value)} placeholder="مثال: تبرعات الجمعة أو تبرع خارجي"/></label><button className="primary" disabled={busy} onClick={add}>{busy?'جارٍ الحفظ...':'إضافة الإيراد'}</button></div><div className="fund-history"><div className="panel-head"><div><h3>سجل الإيرادات</h3><p>لا يتم حذف الحركات المالية حتى يظل الرصيد قابلًا للمراجعة.</p></div></div>{transactions.length===0?<div className="empty">لا توجد إيرادات مسجلة بعد.</div>:<table><thead><tr><th>التاريخ</th><th>النوع</th><th>المبلغ</th><th>البيان</th></tr></thead><tbody>{transactions.map(t=><tr key={t.id}><td>{new Date(t.transaction_date+'T00:00:00').toLocaleDateString('ar-EG')}</td><td>{t.fund_type==='donation'?'تبرعات الجمعة':'مبلغ خارجي'}</td><td>{money(t.amount)}</td><td>{t.description||'—'}</td></tr>)}</tbody></table>}</div></section>}\n`;s=s.slice(0,i)+fn+s.slice(i);}}

if(!s.includes('function FundsDashboardSummary({}){')){const marker='function Dashboard({cases,amounts,select,go}){';const i=s.indexOf(marker);if(i>=0){const fn=`function FundsDashboardSummary({}){const[donation,setDonation]=useState(0),[external,setExternal]=useState(0);useEffect(()=>{Promise.all([supabase.from('fund_transactions').select('amount,transaction_type').eq('fund_type','donation'),supabase.from('fund_transactions').select('amount,transaction_type').eq('fund_type','external'),supabase.from('monthly_payments').select('amount,paid').eq('paid',true)]).then(([a,b,p])=>{const di=(a.data||[]).reduce((n,x)=>n+(x.transaction_type==='income'?Number(x.amount):-Number(x.amount)),0),ei=(b.data||[]).reduce((n,x)=>n+(x.transaction_type==='income'?Number(x.amount):-Number(x.amount)),0),sp=(p.data||[]).reduce((n,x)=>n+Number(x.amount||0),0);setDonation(di-sp);setExternal(ei)})},[]);return <section className="fund-summary"><div><span>رصيد تبرعات الجمعة</span><b>{money(Math.max(0,donation))}</b></div><div><span>رصيد المبالغ الخارجية</span><b>{money(Math.max(0,external))}</b></div></section>}\n`;s=s.slice(0,i)+fn+s.slice(i);}}
if(!s.includes('<FundsDashboardSummary/>')){const i=s.indexOf('function Dashboard({cases,amounts,select,go}){');const ret=s.indexOf('return <>',i);if(ret>=0){const pos=ret+'return <>'.length;s=s.slice(0,pos)+'<FundsDashboardSummary/>'+s.slice(pos)}}

fs.writeFileSync(path,s);console.log('Funds management and external distribution integration injected.');
